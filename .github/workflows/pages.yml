# Publishes web/ to GitHub Pages on every push to main.
# The app lives in web/, and Pages' branch mode only serves the repo root or
# docs/ — docs/ is the written documentation, so we deploy an artifact instead.
#
# web/activinator/ is not an app: it is the hand-off stub for Activinator,
# which used to be served from this repository at /activinator/ and lives in
# StarrySidekick/Activinator now. The stub redirects, and its sw.js takes down
# the worker an installed copy is still running. See the guard in web/sw.js
# and test/deploy.mjs before touching it.
name: Deploy to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish; queue at most one behind it.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web
      - id: deployment
        uses: actions/deploy-pages@v4
